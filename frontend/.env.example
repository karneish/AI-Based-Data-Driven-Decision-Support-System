# Base URL for the DSS-MIP backend API.
# Leave as /api when using the Vite dev proxy (default) or the docker-compose nginx proxy.
# Set to a full URL (e.g. http://localhost:8000/api) to call the backend directly.
VITE_API_BASE_URL=/api
